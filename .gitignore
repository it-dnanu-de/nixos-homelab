# AI memory & credentials — never commit
Memory.md
# TODO/ is version-controlled (project tracker); only the old root TODO.md was private.

# local environment / secrets
.env
*.env
!*.env.example
*.age

# node tooling (only if MCP servers are run locally)
node_modules/

# OS cruft
.DS_Store

# sops — decrypted plaintext outputs must never be committed
*.dec
secrets/*.plain*

# editor
.idea/
.vscode/
.flake.nix

# nix build output symlink
result
